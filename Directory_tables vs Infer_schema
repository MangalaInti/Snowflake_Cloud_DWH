A Directory table which we can enable on External/Internal named stages. It is not a DB object.
It kind of getting the metadata information.
It is not a DB object

CREATE STAGE INT_STAGE
DIRECTORY = (ENABLE = TRUE)

SELECT * FROM DIRECTORY(@INT_STAGE)

--Refresh Directory Tables
ALTER STAGE INT_STAGE REFRESH;
